<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.caipu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cai"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cai"
        android:supportsRtl="true"
        android:theme="@style/mythene">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="login.register.LoginActivity" />
        <activity android:name="login.register.RegisterActivity" />
        <activity android:name=".ShouyeActivity" android:label="@string/my_appName" >

        </activity>
        <activity android:name=".CaipuDetailActivity" android:label="@string/my_detail"></activity>
    </application>

</manifest>